[project]
name = "flarex"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["typer", "scapy", "rich", "pydantic"]

[project.scripts]
flarex = "flarex.__main__:main"

[tool.setuptools]
packages = ["flarex"]